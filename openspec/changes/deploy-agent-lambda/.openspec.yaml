schema: spec-driven
created: 2026-09-03
goal: "Modulo agent-lambda: empaquetado .zip estandar, funcion Lambda Python
  3.13, rol de ejecucion de minimo privilegio (InvokeModel + Retrieve + logs),
  log group; cableado en environments/dev con las env vars de la KB"
