# Terraform
.terraform/
*.tfstate
*.tfstate.*
# Excepción: el state del bootstrap SÍ se versiona (sin secretos; hace el
# bootstrap del remote state reproducible). NO aplica al stack principal.
!terraform/bootstrap/terraform.tfstate
!terraform/bootstrap/terraform.tfstate.backup
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Lambda build output
lambda/dist/

# Env / secrets
.env
.env.*

# OS / editor
.DS_Store
.idea/
.vscode/
